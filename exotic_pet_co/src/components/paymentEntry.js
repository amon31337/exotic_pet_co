import React from 'react';

const paymentEntry = () => {
    let title = 'paymentEntry page';

    return (
        <div>
            <h1>
                {title}
            </h1>
        </div>

    );
};

export default paymentEntry;