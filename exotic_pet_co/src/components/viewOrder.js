import React from 'react';

const viewOrder = () => {
    let title = 'viewOrder page';

    return (
        <div>
            <h1>
                {title}
            </h1>
        </div>

    );
};

export default viewOrder;